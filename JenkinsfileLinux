pipeline {
    agent any

    environment {
        APP_NAME  = 'micro-reserva'
        IMAGE_TAG = "${APP_NAME}:${env.BUILD_ID}"
    }

    stages {
        stage('Verificar Repositório') {
            steps {
                git branch: 'main', url: 'https://github.com/Gugzz21/microsservico_reserva'
            }
        }

        stage('Instalar Dependências') {
            steps {
                sh 'npm install'
                sh 'npx prisma generate'
            }
        }

        stage('Construir Imagem Docker') {
            steps {
                sh "docker build -t ${IMAGE_TAG} ."
            }
        }

        stage('Fazer Deploy') {
            steps {
                // O '|| true' garante que se o container não existir, o Linux não quebra o pipeline
                sh "docker rm -f ${APP_NAME} || true"
                
                sh "docker run -d --name ${APP_NAME} -p 9503:9503 ${IMAGE_TAG}"
            }
        }
    }

    post {
        success {
            echo "Deploy do ${APP_NAME} realizado com sucesso!"
        }
        failure {
            echo "Houve um erro durante o deploy do ${APP_NAME}."
        }
    }
}